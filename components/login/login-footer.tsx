import { ThemedText } from '../ThemedText';
import { ThemedView } from '../ThemedView';

export default function LoginFooter() {
  return (
    <ThemedView className="">
      <ThemedText className="flex items-end justify-end mt-auto">
        Footer
      </ThemedText>
    </ThemedView>
  );
}
